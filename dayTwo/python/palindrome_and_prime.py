

def isPrime(number):
    prime_counter = 0
    for index in (2,101):
        prime_counter = 0
        for number in range(2, index)
            if index % number == 0
                prime_counter += 1
     
    return prime_counter == 2
        
    
def isPalindrome(number) :
    if isPrime(number) :
        return ((number % 10 == (number /10000) % 10) and (number / 10) % 10 == (number / 1000) % 10)
    return False

print(isPalindrome(101)
